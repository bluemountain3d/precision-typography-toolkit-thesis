/**
 * @fileoverview Metrics Output - Public API
 *
 * Barrel export for the MetricsOutput component and its ref interface.
 * This component handles syntax-highlighted code display for font metrics
 * in CSS, SCSS, and JSON formats.
 *
 * Exports:
 * - MetricsOutput: The main component (forwardRef)
 * - MetricsOutputRef: Type interface for the exposed ref methods
 *
 * @module MetricsOutput/index
 */

export { MetricsOutput, type MetricsOutputRef } from './MetricsOutput';
