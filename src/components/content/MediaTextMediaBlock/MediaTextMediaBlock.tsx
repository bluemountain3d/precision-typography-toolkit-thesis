import styles from './MediaTextMediaBlock.module.scss';
import classNames from 'clsx';

interface MediaTextMediaBlockProps {
  
}

export const MediaTextMediaBlock = ({  }: MediaTextMediaBlockProps) => {
  return (
    <div className={classNames(
      styles.mediaTextMediaBlock,
    )}>
      
    </div>
  );
};